---
# install_and_configure_keepalived/defaults/main.yml

systemd_unit_name: 'keepalived.service'
package_name: 'keepalived'
config_file_path: '/etc/keepalived/keepalived.conf'

# keepalived global defaults

vrrp_strict: true

# keepalived VRRP defaults

vrrp_advert_int: 1
vrrp_interface: 'eth0'
